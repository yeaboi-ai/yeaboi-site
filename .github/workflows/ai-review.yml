name: AI Review
on:
  workflow_run:
    workflows: [ci]
    types: [completed]
concurrency:
  group: ai-review-${{ github.event.workflow_run.head_branch }}
  cancel-in-progress: true
permissions:
  contents: read
  pull-requests: write
  issues: read
  actions: read
  id-token: write
jobs:
  claude:
    uses: yeaboi-ai/yeaboi-tooling/.github/workflows/claude-review.yml@139bc95134826dcf4e8838ca949a3215268a3ac1
    secrets:
      CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
# Codex automatic reviews run through its native GitHub integration and ChatGPT subscription.
# Enable them in Codex repository settings; no API key or exported CLI login is used here.
